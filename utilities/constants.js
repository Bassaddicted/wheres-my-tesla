import { light, dark } from '@eva-design/eva';

// Available themes for the application through UI Kitten
const THEMES = {
  light: {
    theme: light,
    icon: 'sun',
  },
  dark: {
    theme: dark,
    icon: 'moon',
  },
};

export { THEMES };
